<odoo>

        <!-- store action window -->
    <record id="wg_job_action" model="ir.actions.act_window">
        <field name="name">Job</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">wgd.job.no</field>
        <field name="view_mode">tree,form</field>
        <field name="help" type="html">
            <p class="oe_view_nocontent_create">
                <!-- Add Text Here -->
            </p>
            <p>
                <!-- More details about what a user can do with this object will be OK -->
            </p>
        </field>
    </record>




  <menuitem
            id="job_menu"
            name="Job"
            action="wg_job_action"
            parent="configuration_menu" sequence="300"/>


</odoo>
